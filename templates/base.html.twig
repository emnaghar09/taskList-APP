<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        <style>
        body {
         
            display: flex;
            flex-direction: column;

            background: #d9a7c7;  /* fallback for old browsers */
            background: -webkit-linear-gradient(to right, #fffcdc, #d9a7c7);  /* Chrome 10-25, Safari 5.1-6 */
            background: linear-gradient(to right, #fffcdc, #d9a7c7); /* W3C, IE 10+/ Edge, Firefox 16+, Chrome 26+, Opera 12+, Safari 7+ */

        }
        .form-container {
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin: auto;
        }
.tasks-container {
    background: #fff;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
    max-width: 900px;
    width: 100%;
    margin: auto;
}
                .btn-custom {
            background-color: #ad5389;
            color: white;
            border: none;
        }
        .btn-custom:hover {
            background-color: #c490b2;
            color: white;
        }
        .navbar{
            margin-bottom:20px;
        }

        #navbarSupportedContent{
        justify-content: space-between

        }
                .footer {
            position: fixed;
            bottom: 50px;
            right: 20px;
            z-index: 1000; /* Ensure it's above other content */
        }
        .footer .btn {
            background-color: #ad5389;
            color: white;
        }
        .footer .btn:hover {
            background-color: #c490b2;
        }
            </style>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>

  <!-- Navbar Always Visible -->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">

    <div class="collapse navbar-collapse" id="navbarSupportedContent">

      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
         {# Afficher uniquement pour ROLE_ADMIN #}
        {% if is_granted('ROLE_ADMIN') %}
          <li class="nav-item">
            <a class="nav-link active" href="/task">Dashboard</a>
          </li>
            <li class="nav-item">
            <a class="nav-link" href="/user">Utilisateurs</a>
          </li>
        {% endif %}
        <li class="nav-item">
          <a class="nav-link " aria-current="page" href="/userTasks">Mes taches</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/new">Ajouter une tache</a>
        </li>
                <li class="nav-item">
          <a class="nav-link" href="/userTaskList">Mes listes</a>
        </li>
      </ul>
    <div class="nav-item">
  <a class="nav-link" href="/logout">Deconnexion</a>
   </div>
    </div>
  </div>
</nav>

        {% block body %}
        
        
        {% endblock %}


        
<footer class="footer ">
        <div class="container">
            <button class="btn btn-light" onclick="window.history.back();">Go Back</button>
        </div>
    </footer>
    </body>
</html>
